const { Category, Item, Profile, User } = require('../models');

const resolvers = {
  Query: {
    items: async ()=> {

    },
    profiles: async () => {

    },
    users: async () => {
    },
    categories: async () => {

    },
  }
};

module.exports = resolvers;